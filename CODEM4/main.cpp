#include <bits/stdc++.h>
using namespace std;
#define sd(a)   scanf("%d",&a)

int arr[40]={0};
int main()
{
    int t;
    sd(t);
    while(t--)
    {
        memset(arr,0,sizeof(arr));
        int i,ans=0,temp=0,n,p1=0,p2=0;
        sd(n);
        for(i=0;i<n;i++)
            sd(arr[i]);


    }
    return 0;
}
