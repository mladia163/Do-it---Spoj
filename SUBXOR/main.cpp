#include <bits/stdc++.h>
using namespace std;
#define ll  long long int
// Using trie ... tobe done later

vector<ll> v;
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        v.clear();
        ll i,j,n,k,temp,ans=0;
        scanf("%lld %lld",&n,&k);
        for(i=0;i<n;i++)
        {
            scanf("%lld",&x);
            v.push_back(x);
        }


    }
    return 0;
}
