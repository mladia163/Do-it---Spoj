#include <bits/stdc++.h>
using namespace std;

int dp[1010][1010];
int main()
{
    char s[1001],t[10001];
    scanf("%s",s);
    while(s[0]!='#')
    {
        scanf("%s",t);
        int i,j,len_s=strlen(s),len_t=strlen(t);

        for(i=0;i<len_t;i++)
            if(s[0])
        for(i=1;i<len_s;i++)
            for(j=0;j<len_t;j++)
            {
                if()

            }






        scanf("%s",s);
    }
    return 0;
}
